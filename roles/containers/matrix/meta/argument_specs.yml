---
argument_specs:
  main:
    short_description: "Continuwuity container."
    author: "dogeystamp <dogeystamp@disroot.org>"
    options:
      matrix_data_path:
        description: "Bind-mount path for Matrix data; this will be created by the role."
        type: "path"
        required: true
      matrix_domain:
        description: "Domain to use for hosting Matrix"
        type: "str"
        required: true
      matrix_server_domain:
        description: "Public domain for Matrix homeserver"
        type: "str"
        required: true
