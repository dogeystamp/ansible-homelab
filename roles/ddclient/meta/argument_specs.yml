---
argument_specs:
  main:
    short_description: "Setup ddclient."
    author: "dogeystamp <dogeystamp@disroot.org>"
    options:
      dyndns_host:
        description: "The last line of ddclient.conf (idk how to explain)"
        type: "str"
        required: true

      dyndns_domain:
        description: "Domain to update DNS for."
        type: "str"
        required: true

      dyndns_password:
        description: "Password for the dyndns service."
        type: "str"
        required: true
